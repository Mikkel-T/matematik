---
import BaseHead from '@components/layout/BaseHead.astro';
import BaseFooter from '@components/layout/BaseFooter.astro';
import BaseHeader from '@components/layout/BaseHeader.astro';
import Toaster from '@components/Toaster.jsx';
const { content } = Astro.props;
---

<html lang="da-DK" class="">
  <head>
    <BaseHead {content} />
  </head>
  <body>
    <header>
      <BaseHeader />
    </header>
    <main>
      {!content.hideTitle && (
        <div class="text-center text-4xl mb-2">{content.title}</div>
      )}
      <slot />
      <Toaster client:visible />
    </main>
    <footer>
      <BaseFooter />
    </footer>
  </body>
</html>
